/** @jsx jsx */
import { jsx } from "theme-ui"

import Title from "../components/title"
import Layout from "../components/layout"

export default ({ data }) => (
  <Layout>
    <div>
      <Title>Account</Title>
    </div>
  </Layout>
)
